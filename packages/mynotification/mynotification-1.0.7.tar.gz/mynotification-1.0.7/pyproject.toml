[project]
name = "mynotification"
version = "1.0.7"
description = "Just a simple library to send desktopnotifications"
authors = [
    { name = "tct123", email = "42028373+tct123@users.noreply.github.com" },
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.poetry]
packages = [{ include = "mynotification", from = "src" }]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
