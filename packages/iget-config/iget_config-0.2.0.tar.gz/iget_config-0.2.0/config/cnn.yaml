num_layers: 1  # 输入通道数
num_blocks: 6  # 残差块的数量, 原始版本为10, v1为5, v2为6
num_filters: 64  # 卷积层的滤波器数, 原始版本为128
num_out_filters: 64  # 残差块的输出通道数
kernel_size: 3  # 卷积核的大小
padding: 1  # 卷积层的填充大小
flat_size: 240  # Value全连接层的输入维度, 计算为 window_size * feature_size
fc1_out_size: 64  # Value全连接层的输出维度, 原始版本为256
train:
  batch_size: 10

