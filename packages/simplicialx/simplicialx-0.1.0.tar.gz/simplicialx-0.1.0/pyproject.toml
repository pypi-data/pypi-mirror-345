[project]
name = "simplicialx"
version = "0.1.0"
license = "GPL-2.0-only"

authors = [
  { name="Alexander Serebrennikov" },
  { name="Oleg Kachan" }
]

dependencies = [
    "numpy",
    "networkx",
    "sympy"
]

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"