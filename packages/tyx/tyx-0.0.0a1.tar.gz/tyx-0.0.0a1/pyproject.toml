[project]
name = "tyx"
version = "0.0.0a1"
description = "This project is taken to defend against confusion with `ty`."
readme = "README.md"
authors = [
    { name = "Zanie Blue", email = "contact@zanie.dev" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
tyx = "tyx:main"

[build-system]
requires = ["uv_build>=0.7.2,<0.8"]
build-backend = "uv_build"
