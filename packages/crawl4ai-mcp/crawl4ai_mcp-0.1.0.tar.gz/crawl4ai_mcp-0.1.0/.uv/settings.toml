[uv]
# Use system-level certificates 
use-system-certs = true

# Configure pip fallback behavior
pip-config = true

# Use parallel downloads
parallel = true

# Set the virtualenv location to be inside the project
virtualenv = ".venv" 