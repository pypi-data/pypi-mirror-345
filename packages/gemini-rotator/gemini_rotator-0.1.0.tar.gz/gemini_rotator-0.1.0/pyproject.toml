[project]
name = "gemini-rotator"
version = "0.1.0"
description = "Key rotator, don't get dizzy."
readme = "README.md"
authors = [
    { name = "johnmalek312", email = "77510343+johnmalek312@users.noreply.github.com" }
]
requires-python = ">=3.9"
dependencies = ["google-auth"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
