from app.models import db
from app.models.mixin import TimestampMixin

class {{ model_name }}(db.Model, TimestampMixin):
    __tablename__ = "{{ table_name }}"

    id = db.Column(db.Integer, primary_key=True)
    # Add your fields here