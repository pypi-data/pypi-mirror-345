[project]
name = "mcp-demo"
version = "0.1.0"
description = "A MCP server project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "mcp>=1.7.1",]
[[project.authors]]
name = "DevinWu"
email = "wuhaifengdhu@gmail.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
mcp-demo = "mcp_demo:main"
