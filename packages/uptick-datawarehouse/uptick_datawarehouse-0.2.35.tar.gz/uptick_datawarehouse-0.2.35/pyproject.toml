[project]
name = "uptick-datawarehouse"
version = "0.2.35"
description = "Uptick Data Warehouse Schemas"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=3.2, <5.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
