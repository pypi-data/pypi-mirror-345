[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mseep_mseep-metabase-mcp"
version = "0.1.0"
description = "Package published under mseep brand"
authors = [
    {name = "mseep", email = "support@skydeck.ai"}
]
requires-python = ">=3.6"

[tool.setuptools]
packages = ['src']
