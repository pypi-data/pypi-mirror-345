[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pykit"]

[project]
name = "r00pykit"
version = "0.1.3"
requires-python = ">=3.12"
dependencies = [
    "r00logger",
]

