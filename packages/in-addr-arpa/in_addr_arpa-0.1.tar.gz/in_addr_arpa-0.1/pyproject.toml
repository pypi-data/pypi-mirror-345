[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "in_addr_arpa"
authors = [{name = "Julien Palard", email = "julien@palard.fr"}]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
dynamic = ["version", "description"]

[project.urls]
Home = "https://git.afpy.org/mdk/in_addr_arpa"

[project.scripts]
"in-addr.arpa" = "in_addr_arpa:main"

[black]
