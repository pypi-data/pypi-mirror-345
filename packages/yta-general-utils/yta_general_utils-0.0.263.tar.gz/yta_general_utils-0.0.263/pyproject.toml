[project]
name = "yta-general-utils"
version = "0.0.263"
description = "Youtube Autónomo general utils are here."
authors = [
    {name = "danialcala94",email = "danielalcalavalera@gmail.com"}
]
readme = "README.md"
requires-python = "==3.9"
dependencies = [
    "moviepy (>=2.0.0,<3.0.0)",
    "pillow (>=10.4.0,<11.0.0)",
    # I think I'm not using this one, but this was the version I had installed and it should go in the 'yta_image' package if needed
    "scikit-image (>=0.22.0,<1.0.0)",
    "numpy (>=1.22.0,<3.0.0)", # Previously 1.26.4
    # Please, remove this 'pydub' library if possible
    "pydub (>=0.25.1,<1.0.0)",
    "exrex (>=0.12.0,<1.0.0)",
    "lxml (>=5.2.2,<6.0.0)",
    "python-dotenv (>=1.0.0,<2.0.0)",
    "requests (>=2.32.3,<3.0.0)",
    "yta_validation (>=0.0.1,<1.0.0)"
]

[tool.poetry]
packages = [{include = "yta_general_utils", from = "src"}]

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
