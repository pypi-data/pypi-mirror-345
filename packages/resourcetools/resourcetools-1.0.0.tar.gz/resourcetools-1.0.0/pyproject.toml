[project]
name = "resourcetools"
version = "1.0.0"
description = "A lightweight Python package for managing application resources (icons, images, translations, etc.) in a clean, importlib-compatible way — ideal for PySide6 and other GUI or CLI apps."
authors = [
    {name = "Rafal Padkowski", email = "rafaelp@poczta.onet.pl"},
]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
