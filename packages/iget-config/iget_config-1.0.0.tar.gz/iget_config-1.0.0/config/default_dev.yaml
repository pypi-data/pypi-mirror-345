train:
  batch_size: 512
  num_epochs: 100
data:
  window_size: 20
  label_size: 1
  label_exclude_days: 1
  label_mode: 2

Adam:
  lr: 0.002 # DEV 覆盖值

OneCycleLR:
  max_lr: 0.05 # DEV 覆盖值