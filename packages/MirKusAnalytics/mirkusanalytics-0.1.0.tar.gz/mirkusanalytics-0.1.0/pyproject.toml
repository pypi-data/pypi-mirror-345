[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "MirKusAnalytics"
version = "0.1.0"
description = "Private analytics tools"
authors = [{name = "MirKus Inc.", email = "information@mirkus.com"}]
readme = "README.md"
license = {text = "MIT"}
dependencies = [
    "pandas",
    "SQLAlchemy",
    "python-dotenv",
    "pyodbc"
]
