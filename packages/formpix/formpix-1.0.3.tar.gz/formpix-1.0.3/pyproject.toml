[project]
name = "formpix"
version = "1.0.3"
description = "Client library for interacting with the FormPix API"
authors = [{ name = "C. Smith", email = "csmith@ytech.edu" }]
readme = "README.md"
license = { text = "MIT" }
dependencies = ["requests"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
