[project]
name = "talkit-agent"
version = "0.1.2"
description = "Talkit Agent turns any OpenAPI spec into a conversational API assistant. It understands user prompts, picks the right endpoint, makes the call, and returns the result. Just talk to your API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai-agents>=0.0.14",
    "pytest>=8.3.5",
    "ruff>=0.11.8",
]
