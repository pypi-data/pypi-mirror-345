[project]
name = "mcp-connection-checker"
version = "0.1.0"
description = "MCPサーバーと対話し、定義されたツールを実行するためのコマンドラインインターフェース。"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    # 依存関係はここに記述します
    # 例: "requests >= 2.28.1"
]

[project.scripts]
mcp-connection-checker = "test_mcp_connection:main"

[build-system]
requires = ["setuptools", "wheel"] # パッケージングに必要なツール
build-backend = "setuptools.build_meta"
