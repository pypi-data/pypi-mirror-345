[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "adesinastreets"
version = "0.0.1"
description = "adesinastreets is a python package that gives the list of all the streets in the world offline"
readme = "README.md"
authors = [{ name="Abdullah Adesina Dhikrullah", email="daadesina1@gmail.com" }]
license = { file = "LICENSE" }
dependencies = []
requires-python = ">=3.10"
