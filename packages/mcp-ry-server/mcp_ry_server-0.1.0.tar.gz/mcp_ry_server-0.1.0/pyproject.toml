[project]
name = "mcp-ry-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ry", email = "" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.7.1",
]

[project.scripts]
mcp-ry-server = "mcp_ry_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
