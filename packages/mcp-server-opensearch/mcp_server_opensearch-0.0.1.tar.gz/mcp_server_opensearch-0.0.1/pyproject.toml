[project]
name = "mcp-server-opensearch"
version = "0.0.1"
description = "mcp-server-opensearch"
authors = [{ name = "OpenSearch Contributors", email = "release@opensearch.org" }]
license = { text = "Apache-2.0" }
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
