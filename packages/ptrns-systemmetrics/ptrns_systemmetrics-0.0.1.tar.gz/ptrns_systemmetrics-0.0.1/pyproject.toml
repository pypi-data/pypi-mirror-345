[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ptrns-systemmetrics"
version = "0.0.1"
description = "A lightweight system metrics collector"
authors = [{ name = "kaniMozhi", email = "kaniMozhiRam@gmail.com" }]
license = { text = "MID" }
dependencies = [
    "psutil"
]

[project.urls]


[tool.setuptools]

