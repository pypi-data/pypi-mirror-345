[project]
name = "authx-sdk"
version = "0.1.0"
description = "AuthX SDK for OAuth2 token management"
authors = [
  { name = "Fawwaz Ali" }
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["requests"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
