[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "universal_encryptor"
version = "0.1.0"
description = "Universal file encryptor and decryptor with GUI"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

authors = [
  { name="Jean Cotto", email="jccs22_26@outlook.com" }
]

dependencies = [
  "customtkinter",
  "Pillow",
  "filetype",
  "cryptography"
]
