[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sourcemix-sdk"
version = "0.2.0"
description = "SDK for interacting with SourceMix agents"
authors = [{name = "Ferdinand Messens", email = "ferdinandmessens@gmail.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
dependencies = ["requests"]

