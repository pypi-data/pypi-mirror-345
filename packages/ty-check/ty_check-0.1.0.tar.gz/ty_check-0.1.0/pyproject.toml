[project]
name = "ty-check"
version = "0.1.0"
description = "This project is taken to defend against confusion with `ty check`."
readme = "README.md"
authors = [
    { name = "Zanie Blue", email = "contact@zanie.dev" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ty-check = "ty_check:main"

[build-system]
requires = ["uv_build>=0.7.2,<0.8"]
build-backend = "uv_build"
