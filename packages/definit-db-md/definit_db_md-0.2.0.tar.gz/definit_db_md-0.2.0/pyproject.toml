[project]
name = "definit-db-md"
version = "0.2.0"
description = "DefinIT Markdown database"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["definit_db_md"]
include-package-data = true

[tool.setuptools.package-data]
"definit_db_md" = ["data/*"]
