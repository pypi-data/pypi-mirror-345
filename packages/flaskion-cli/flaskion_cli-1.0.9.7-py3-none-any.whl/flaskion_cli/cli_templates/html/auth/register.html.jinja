<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Register - Flaskion</title>
    <style>
        body {
            background-color: #253659;
            font-family: Arial, sans-serif;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .auth-container {
            background-color: #2f416d;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 20px rgba(0,0,0,0.2);
            max-width: 400px;
            width: 100%;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #04BF9D;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-top: 10px;
            font-weight: bold;
        }

        input {
            padding: 10px;
            margin-top: 5px;
            border: none;
            border-radius: 4px;
        }

        button {
            margin-top: 20px;
            padding: 12px;
            background-color: #04BF9D;
            border: none;
            color: white;
            font-size: 1em;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        button:hover {
            background-color: #039f82;
        }

        .link {
            text-align: center;
            margin-top: 15px;
        }

        .link a {
            color: #BF665E;
            text-decoration: none;
        }

        .link a:hover {
            text-decoration: underline;
        }

        ul {
            padding-left: 20px;
            color: #f76c5e;
        }

        li {
            margin-bottom: 8px;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <h2>Register</h2>

        {% raw %}
        {% with messages = get_flashed_messages() %}
          {% if messages %}
            <ul>
              {% for message in messages %}
                <li>{{ message }}</li>
              {% endfor %}
            </ul>
          {% endif %}
        {% endwith %}
        {% endraw %}

        <form method="POST">
            <label>Email:</label>
            <input type="email" name="email" required>

            <label>Password:</label>
            <input type="password" name="password" required>

            <button type="submit">Register</button>
        </form>

        {% raw %}
        <div class="link">
            <p>Already have an account? <a href="{{ url_for('web_routes.login') }}">Login here</a></p>
        </div>
        {% endraw %}
    </div>
</body>
</html>