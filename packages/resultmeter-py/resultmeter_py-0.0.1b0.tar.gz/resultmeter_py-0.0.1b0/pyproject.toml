[project]
name = "resultmeter-py"
version = "0.0.1b0"
description = ""
authors = [
    {name = "Vedat Dogan",email = "vedat202dogan@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]

[tool.poetry]
packages = [{include = "resultmeter_py", from = "src"}]


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[tool.pytest.ini_options]
pythonpath = ["src"]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
