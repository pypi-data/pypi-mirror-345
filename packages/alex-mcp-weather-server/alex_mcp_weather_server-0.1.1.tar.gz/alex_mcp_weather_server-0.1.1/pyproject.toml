[project]
name = "alex-mcp-weather-server"
version = "0.1.1"
description = "输入OpenWeather-API-KEY，获取天气信息"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.7.0",
]
[project.scripts]
mcp-server-weather = "main:main"