from setuptools import setup, find_packages

setup(
    name="dc_ci",
    version="0.1.0",
    packages=find_packages(),
    description="DC CI importable module with multiple files",
    author="Your Name",
)
