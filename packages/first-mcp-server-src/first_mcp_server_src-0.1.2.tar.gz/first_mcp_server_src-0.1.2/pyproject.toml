[project]
name = "first-mcp-server-src"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sakthivel Chellapparimanam", email = "srcsak@amazon.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.7.0",
]

[project.scripts]
weather-mcp = "first_mcp_server_src.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
