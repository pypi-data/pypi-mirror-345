[project]
name = "ok-subprocess-defaults"
version = "0.1"
description = "Minor wrapper for subprocess.run() with defaults and logging"
readme = "README.md"
authors = [{ name = "Dan Egnor", email="egnor@ofb.net" }]
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Homepage = "https://github.com/egnor/ok-py-subprocess-defaults#readme"
Repository = "https://github.com/egnor/ok-py-subprocess-defaults.git"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
