[project]
name = "jkhbfea"
version = "0.2.0"
description = "Une simple bibliothèque pour faire des calculs et d'autres fonctions utilitaires."
authors = [
    { name = "jkhbfea" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"

dependencies = [
    "cryptography"
]

classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
