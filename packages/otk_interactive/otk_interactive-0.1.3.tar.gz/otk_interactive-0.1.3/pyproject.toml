[project]
name = "otk_interactive"
version = "0.1.3"
description = ""
authors = [
    {name = "Kennete Basco",email = "97374837+kankenny@users.noreply.github.com"}
]
license = { text = "MIT License" }
requires-python = ">=3.10,<3.13"

dependencies = []
tensorflow-io-gcs-filesystem = { version = "^0.37.1", markers = "sys_platform != 'win32'" }

[project.scripts]
otk_interactive = "otk_interactive:main"

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
flake8 = "^7.1.2"
isort = "^6.0.1"
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"

[tool.black]
exclude = '''
/(
    \.eggs
  | \.git
  | \.venv
  | \.env
  | build
  | dist
  | \.mypy_cache
)\
'''

[tool.commitizen]
version = "0.1.3"
tag_format = "v$version"
version_files = [
  "pyproject.toml:version",
  "otk_interactive/__version__.py"
]
