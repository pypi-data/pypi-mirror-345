[project]
name = "engines4ai"
version = "0.1.1"
description = "This is a SearXNG hepler for Python."
readme = "README.md"
requires-python = ">=3.7"
license = { file = "LICENSE" }
authors = [
  { name="Keisuke Miyamoto", email="miyamotokeisuke0727@gmail.com" }
]
dependencies = [
  "requests"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

