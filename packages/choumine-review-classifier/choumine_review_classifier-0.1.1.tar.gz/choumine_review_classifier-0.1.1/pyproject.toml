[project]
name = "choumine-review-classifier"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "choumine", email = "569908115@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.6.0",
    "openai>=1.76.2",
    "pandas>=2.2.3",
    "pyyaml>=6.0.2",
]

[project.scripts]
choumine-review-classifier = "choumine_review_classifier:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
