[project]
name = "youtubeinsights-mcp-server-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hanalia", email = "dabidstudio08@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "build>=1.2.2.post1",
    "mcp>=1.7.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
    "twine>=6.1.0",
    "youtube-transcript-api>=1.0.3",
]

[project.scripts]
youtubeinsights-mcp-server = "youtubeinsights_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
