[build-system]
requires = ["setuptools>=67.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "curious-machine"
version = "0.0.1"
authors = [
    {name="Alexander Fedotov", email="alex.fedotov@aol.com"},
]
description = "A Machine that is curious."
readme = "README.md"
requires-python = ">=3.10"
classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
]
dependencies = [
    "PyGithub >= 2.6.1",
    "PyYAML >= 6.0.2"
]
[project.urls]
"Homepage" = "https://github.com/curious-machine/curious-machine"
"Bug Tracker" = "https://github.com/curious-machine/curious-machine/issues"