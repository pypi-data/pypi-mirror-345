[project]
name = "ssl-common-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ssl", email = "simplystronglab@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ruff>=0.11.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
