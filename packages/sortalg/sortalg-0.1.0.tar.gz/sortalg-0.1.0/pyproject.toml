[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["sortalg"]
package-dir = {"" = "src"}

[project]
name = "sortalg"       # Must be unique on PyPI
version = "0.1.0"
description = "a collection of sorting algorithms in python"
authors = [
  { name="Morteza Salarkia", email="msalarkia3@gmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[project.urls]

