[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "OpentronsHTTPAPIWrapper"
dynamic = ["version"]
authors = [
  {name = "Daniel Persaud"},
  {name = "Nis Fisker-Bødker"},
]
description = "Python wrapper for the opentrons http api."
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests"]

license = {text = "CC0-1.0"}

[project.urls]
Homepage = "https://github.com/dpersaud/opentronsHTTPAPI_wrapper"
Repository = "https://github.com/dpersaud/opentronsHTTPAPI_wrapper"
