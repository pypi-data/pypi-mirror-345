[project]
name = "unoff-qweather-mcp"
version = "0.1.2"
description = "An unofficial MCP (Multiagent Conversation Protocol) wrapper for the QWeather API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.21.0",
    "mcp>=1.7.0",
]
[project.scripts]
unoff-qweather-mcp = "unoff_qweather_mcp:main"