from setuptools import setup, find_packages

setup(
    name='okton',
    version='0.1',
    packages=find_packages(),
    install_requires=['kivy'],
    description='Une bibliothèque simple pour créer des apps mobiles en Python',
)
