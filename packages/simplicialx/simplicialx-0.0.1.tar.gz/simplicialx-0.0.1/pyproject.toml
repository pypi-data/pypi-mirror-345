[project]
name = "simplicialx"
version = "0.0.1"
license = "GPL-2.0-only"

dependencies = [
    "numpy",
    "networkx",
    "sympy"
]

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"